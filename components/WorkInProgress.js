export default function WorkInProgress() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        padding: "2rem",
        fontFamily: "system-ui, sans-serif",
        textAlign: "center",
        background: "#fafafa",
        color: "#333",
      }}
    >
      <p style={{ fontSize: "1rem", color: "#666" }}>Work in progress.</p>
    </div>
  );
}
